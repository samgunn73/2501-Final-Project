#include "objective_game_object.h"

namespace game {

    ObjectiveGameObject::ObjectiveGameObject(const glm::vec3& position,
        Geometry* geometry, Shader* shader, GLuint texture)
        : GameObject(position, geometry, shader, texture){
    }

    




}